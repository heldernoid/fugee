# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
dist/
*.egg-info/

# Node
node_modules/
agent/node_modules/

# Generated documents (ephemeral)
tmp/
*.pdf
# …but DO keep the source UNHCR guideline PDFs — they're the provenance of the
# RAG index (build_guidelines_index.py) and small enough for plain git here.
!specs/data/guidelines/*.pdf

# Environment
.env
.env.local
.env.modal

# OS
.DS_Store
Thumbs.db

# Test artifacts
.pytest_cache/
htmlcov/
.coverage

# UNHCR download artifacts (regenerate with data/scripts/unhcr_downloader.py)
data/raw/*.json
data/processed/*.json

# Enriched countries file is committed so the app/Space is ready without running
# the pipeline (it's small; regenerate with data/scripts/enrich_downloader.py).

# Guidelines RAG index is committed here (23 MB, under GitHub's 100 MB limit) so
# the repo is self-contained. Regenerate with data/scripts/build_guidelines_index.py
